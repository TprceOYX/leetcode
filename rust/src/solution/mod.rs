struct Solution {}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let s = String::from("abcdef");
        let t = String::from("cdefab");
        // println!("res: {}", Solution::rotate_string(s, t))
    }
}
